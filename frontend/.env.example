# Local development default is http://localhost:8000 if this variable is omitted.
# Staging/production builds must define the deployed backend URL.
EXPO_PUBLIC_API_URL=https://<render-service>.onrender.com
